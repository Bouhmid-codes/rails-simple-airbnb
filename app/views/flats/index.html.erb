<!-- display the flats cards -->
<div class="container">
  <div class="row">
    <% @flats.each do |flat| %>
      <div class="col-md-3 mt-2">
        <%= render 'card', flat: flat %>
      </div>
    <% end %>
  </div>
</div>
